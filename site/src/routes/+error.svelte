<script lang="ts">import { base } from '$app/paths'; import { page } from '$app/state';</script><svelte:head><title>Page not found · Torbie</title></svelte:head><div class="wrap error-page"><h1>{page.status === 404 ? 'Page not found.' : 'This page could not load.'}</h1><p>The address may have changed. The feature catalogue and downloads are a step away.</p><a class="button" href={`${base}/`}>Back to Torbie</a><a href={`${base}/features/`}>Browse features</a></div>
